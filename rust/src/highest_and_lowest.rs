pub fn high_and_low(numbers: &str) -> String {
  let numbers_vec = vec!(numbers.split(" "));
  print!("{:?}", numbers_vec);
  print!("len = {}", numbers_vec.len());
//   print!("{}", &numbers_vec[0]);
  return String::from("");
}